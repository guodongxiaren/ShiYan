规范化
=======
NF即范式，1NF被称为第一范式。其他NF同理。
##1NF
满足最低要求的就是第一范式（1NF）
##2NF
在1NF的基础上消除了部分函数依赖，使得所有非主属性都完全函数依赖与码。
##3NF
在2NF的基础上消除了非主属性对主属性的传递函数依赖。
##BCNF
在3NF的基础上消除了所有属性对主属性的传递函数依赖。
##4NF
若只考虑**函数依赖**，则修正后的3NF（即BCNF）的规范化程度已经是最高的了。  
而4NF则辟蹊径，从**多值依赖**的角度来进行规范化。

